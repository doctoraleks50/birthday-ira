Put peony.glb here (Sketchfab: peony-e5aacbac020d483094b110e11dd9b306 by PBR3D).

That Sketchfab listing is a paid Standard model (~$20). After purchase/download,
export or convert to GLB and save as:

  public/assets/models/peony.glb

The site clones it into a bouquet with clear film wrap.
